#include "main.h"

/**
 * _abs - Write a function that computes the absolute value of an integer
 * @num: variable to test 
 * Return: Nothing
 */

int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (-num);
}
